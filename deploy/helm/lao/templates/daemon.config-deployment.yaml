apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: daemon.config
  labels:
    app: daemon.config
    provider: lao
    group: com.laegler.lao
    project: daemon.config
    version: {{ .Values.lao.version }}
spec:
  replicas: 1
  revisionHistoryLimit: {{ .Values.upgrade.revisionHistoryLimit }}
  minReadySeconds: {{ .Values.upgrade.minReadySeconds }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: {{ .Values.upgrade.maxUnavailable }}
  template:
    metadata:
      labels:
        app: daemon.config
    spec:
      containers:
      - name: daemon.config
        image: {{ .Values.lao.registry }}/daemon.config:{{ .Values.lao.version }}
        imagePullPolicy: Always
        env:
        - name: SPRING_CLOUD_CONFIG_GIT_LABEL
          value: {{ .Values.spring.cloud.config.git.label }}
        ports:
        - name: api
          containerPort: 8888
      restartPolicy: Always
      terminationGracePeriodSeconds: 30