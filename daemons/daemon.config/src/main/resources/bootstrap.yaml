spring:
  application:
    name: @project.artifactId@
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thlaegler/lao-logistics-config.git
          username: changeme
          password: changeme